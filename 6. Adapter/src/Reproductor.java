// Target
// Reproductor es la interfaz que nuestro sistema espera usar. El cliente solo conoce esta interfaz.

public interface Reproductor {

    // Método que el cliente quiere utilizar.
    void reproducir();
}
